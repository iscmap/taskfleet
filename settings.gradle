rootProject.name = "taskfleet"

include(
        "services:order-service",
        "services:customer-service",
        "services:import-service",
        "services:notification-service",
        "libs:common",
        "libs:contracts"
)